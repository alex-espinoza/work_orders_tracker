<h1>Teams#index</h1>
<%= link_to "Create new team", new_team_path %>

<h3>Teams</h3>
	<table>
		<tr>
			<td>Name</td>
		</tr>
		<% @team.each do |team| %>
		<tr>
			<td><%= link_to team.team_name, team_path(team) %></td>
		</tr>
		<% end %>
	</table>
