# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  hello: "Hello world"
  errors:
    messages:
      wrong_size: "size is the wrong size (should be %{file_size})"
      size_too_small: "size is too small (should be at least %{file_size})"
      size_too_big: "size is too big (should be at most %{file_size})"
